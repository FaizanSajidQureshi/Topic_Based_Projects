🧩 Step 1: Install the library

In your terminal or Anaconda prompt (same environment where you run Streamlit), run:

pip install fpdf


If you’re using Anaconda, you can also try:

conda install -c conda-forge fpdf

💡 Step 2: Verify installation

After installation, test it quickly:

python -m pip show fpdf


You should see output like:

Name: fpdf
Version: 1.7.2

🚀 Step 3: Run your Streamlit app
streamlit run app.py
